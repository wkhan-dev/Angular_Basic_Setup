import { ImpurePipeExamplePipe } from './impure-pipe-example.pipe';

describe('ImpurePipeExamplePipe', () => {
  it('create an instance', () => {
    const pipe = new ImpurePipeExamplePipe();
    expect(pipe).toBeTruthy();
  });
});
